module.exports = {

	/**
	 * Mode.
	 *
	 * `development` or `production`.
	 */
	mode: 'development',

	/**
	 * The settings object name.
	 *
	 * It should be the same as the one used with `wp_localize_script` or
	 * `WPMyAds::enqueue_asset`
	 */
	settingsObjectName: 'classifieds',
}
